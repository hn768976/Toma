# Deliverables — AI Brain Over Circuit Plane

Rendered from `../ai-brain-plane` (Remotion). The compositions are defined at
3840x2160; these previews are `--scale=0.5`.

| File | What |
|---|---|
| `V1_AIBrainPlaneBlue.mp4` | V1, deep blue. 1920x1080, H.264 yuv420p (limited range, BT.709), 30 fps, 20 s, no audio. |
| `V2_AIBrainPlaneMono.mp4` | V2, silver/white monochrome. Same format. Verified R = G = B on every pixel. |
| `V1_AIBrainPlaneBlue.png` | 1080p still, frame 400. |
| `V2_AIBrainPlaneMono.png` | 1080p still, frame 400. |
| `ai-brain-plane-project.zip` | The whole Remotion project, ready to render at 4K elsewhere. Source, `remotion.config.ts`, pinned `package.json` + lockfile, `README.md`. No `node_modules`, no `.git`, no render output. |

4K is rendered separately — the command per composition is in the project
README inside the zip.
