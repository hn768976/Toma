# Fonts

`RobotoMono-Regular.woff2` is Roboto Mono 400 (latin subset), served by Google
Fonts and licensed under the Apache License 2.0.

It is vendored rather than fetched at render time so a render never depends on
network access — see `src/board/font.ts`.
