# Vendored from ~/projects/remotion-lib

These files are copies, not edits. Change them in the library first, re-copy,
then confirm the previews are unchanged. See ../../README.md.

Files: color.ts, seeded.ts, dofBuffers.ts, marks.ts, postFx.ts,
TiltedPlane.tsx, PanelChrome.tsx
