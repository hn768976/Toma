/**
 * Timing and format constants for the satin-waves loop.
 *
 * These match the reference clip exactly: 10.000 s at 30 fps.
 */

export const FPS = 30;

/** 300 frames = 10.000 s, the same length as the reference footage. */
export const DURATION_IN_FRAMES = 300;

export const HD_WIDTH = 1920;
export const HD_HEIGHT = 1080;

export const UHD_WIDTH = 3840;
export const UHD_HEIGHT = 2160;
