// Self-hosted webfonts. Bundled from node_modules so a render never depends
// on a network fetch, which keeps output deterministic on CI and offline.
import "@fontsource-variable/inter";
import "@fontsource/jetbrains-mono/400.css";
import "@fontsource/jetbrains-mono/500.css";
