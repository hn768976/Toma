node_modules

# Build output. The standalone explainer is committed so the finished page is
# available without a build step.
dist/*
!dist/kurzgesagt-vault.html
!dist/minutephysics-alarm.html
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out
