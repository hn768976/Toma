/** Both compositions are authored at 4K so they can be rendered at 4K later. */
export const WIDTH = 3840;
export const HEIGHT = 2160;
export const FPS = 30;
/** 14 seconds. Every animation schedule returns to its frame-0 state here. */
export const DURATION_IN_FRAMES = 420;
